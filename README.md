# calirko-bin

A file/paste sharing service built with SvelteKit and MinIO.

## Setup

Copy `.env.example` to `.env` and fill in the values.

## Dev

```sh
bun install
bun run dev
```

## Deploy

```sh
docker compose up -d
```
